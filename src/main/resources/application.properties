spring.application.name=gidisoft

server.port=8080

spring.datasource.url = jdbc:postgresql://${DATABASE_WRITERENDPOINT:localhost}:${DATABASE_PORT:5433}/${DATABASE_NAME:gidisoft}
#spring.datasource.url = jdbc:postgresql://${DATABASE_WRITERENDPOINT:localhost}:${DATABASE_PORT:5432}/${DATABASE_NAME:gidisoft}
spring.datasource.username = ${SPRING_USERNAME:postgres}
spring.datasource.password = ${SPRING_PASSWORD:1379}
spring.datasource.driver-class-name = org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
server.error.include-stacktrace=never
spring.data.jdbc.repositories.enabled=false
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true
spring.jpa.properties.hibernate.format_sql = false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation = true

spring.thymeleaf.enabled=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

gidisoft.password-admin=${ADMIN_PASSWORD:admin123.}
gidisoft.password-users=${DEFEAT_PASSWORD:user123.}

#spring.mail.host=${MAIL_HOST:smtp.gmail.com}
#spring.mail.port=${MAIL_PORT:587}
#spring.mail.username=${EMAIL:gidisoft.upfs@gmail.com}
#spring.mail.password=${MAIL_PASSWORD:rplq advl llwe wiyt}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

spring.cloudinary.api-key=${API_KEY_CLOUD:922515417818213}
spring.cloudinary.api-secret=${API_SECRET_CLOUD:neY2mR5dX_Ojeadfk0WnqC5Ok7A}
spring.cloudinary.api-cloud-name=${API_CLOUD_NAME:de7vnk1ke}

spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always

spring.servlet.multipart.max-file-size=15MB
spring.servlet.multipart.max-request-size = 15MB
